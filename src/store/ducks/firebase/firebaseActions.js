// import { createAction } from 'redux-actions';
// import * as firebaseTypes from './firebaseTypes';

// export const firstAction = createAction(firebaseTypes.FIRST_ACTION);
// export const secondAction = createAction(firebaseTypes.SECOND_ACTION);
